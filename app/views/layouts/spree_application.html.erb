<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <%= hook :inside_head do %>
      <%= render :partial => 'shared/head'%>
    <% end %>
    <%= yield(:slider_head) -%>
  </head>

  <%
    if @body_class.nil?
      if content_for?(:sidebar)
        @body_class = 'two-col'
      else
        @body_class = 'one-col'
      end
    end
  %>

  <body class="<%= @body_class %>" id="<%= @body_id %>">
      <div class="container_cabe"><!--container cabe añadida para que la cabecera cuadre con el cuerpo-->
				<div id="relleno"></div>
				<div id="menu_cab">
					<div id="inicio_link"><%= link_to "INICIO" , root_path %></div>
					| <%= link_to " Blog" , "http://bestiaz.wordpress.com/" %>
 					| <%= link_to " Quienes somos" , pages_who_we_are_path %>
 					| <%= link_to " Nuestros Atletas" , pages_our_athletes_path %>
 					| <%= link_to "Bdev" , pages_bdev_path %>

				</div>
      </div>
    <br />
    <div id="wrapper" class="container">
		
		<%= render :partial => 'shared/login-bar' %>

    <% if content_for?(:sidebar) %>
      <div id="sidebar">
        <%= hook :sidebar do %>
          <%= yield :sidebar %>
        <% end %>
      </div>
    <% end %>

    <div id="content">

      <%= breadcrumbs(@taxon) if @taxon %>
      <% if flash.notice %>
        <div class="flash notice"><%= flash.notice %></div>
      <% end %>
      <% if flash[:error] %>
        <div class="flash errors"><%= flash[:error] %></div>
      <% end %>
      <%= yield %>
    </div>
    <%= render :partial => 'shared/footer'%>
    <% unless content_for?(:sidebar) %>
      <%= render :partial => 'shared/marcas'%>
    <% end %>
  </div>

</body>
</html>
